apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: psql-pvc
spec:
    accessModes: [ "ReadWriteOnce" ]
    resources:
        requests:
            storage: 100Mi
    storageClassName: microk8s-hostpath
