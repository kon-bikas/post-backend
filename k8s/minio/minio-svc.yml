apiVersion: v1
kind: Service
metadata:
    name: minio-svc
    namespace: default
    labels:
        app: minio
spec:
    selector:
        app: minio
    type: ClusterIP
    ports:
        - name: main-port
          protocol: TCP
          port: 9000
          targetPort: minio-port
